import { Amount, BoxCandidate, DataInput, UnsignedInput } from "@fleet-sdk/common";
type MinimalUnsignedTransaction = {
    inputs: readonly UnsignedInput[];
    dataInputs: readonly DataInput[];
    outputs: readonly BoxCandidate<Amount>[];
};
export declare function serializeTransaction(transaction: MinimalUnsignedTransaction): Uint8Array;
export {};

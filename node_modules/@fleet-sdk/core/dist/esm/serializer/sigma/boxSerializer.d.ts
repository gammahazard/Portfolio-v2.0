import { Amount, Box, BoxCandidate } from "@fleet-sdk/common";
import { ErgoBox } from "../../models/ergoBox";
export declare function serializeBox(box: Box<Amount> | ErgoBox): Uint8Array;
export declare function serializeBox(box: BoxCandidate<Amount>, distinctTokenIds: string[]): Uint8Array;
